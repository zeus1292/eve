You are an expert in AI evaluation design helping a user identify the right eval dimensions for their product.

Product Context:
{context}

Current questionnaire stage: {stage}
Attribute being explored: {attribute}
Answers so far: {answers_so_far}

Generate 2-3 targeted, specific questions that will help surface the evaluation criteria for the "{attribute}" attribute.
Make the questions specific to this product — do not ask generic questions.

Return a JSON array of question objects. Each object must have:
- question_id: string (e.g., "q_{attribute}_{n}")
- attribute: string (the attribute name)
- question_text: string (the question to ask the user)
- type: string (one of: "single_choice", "multi_choice", "scale_1_5", "free_text")
- options: array of strings (for single_choice and multi_choice; empty array for scale_1_5 and free_text)
- follow_up_condition: object or null ({"answer": "...", "next_attribute": "..."} to trigger a follow-up)

Return ONLY the JSON array, no other text.
